#include <stdio.h>

/* A padaria Hotpão vende uma certa quantidade de pães franceses e uma quantidade de broas a cada dia. Cada pãozinho custa R$ 0,12 e a broa custa R$ 1,50. Ao final do dia, o dono quer saber
quanto arrecadou com a venda dos pães e broas (juntos), e quanto deve
guardar numa conta de poupança (10% do total arrecadado). Você foi
contratado para fazer os cálculos para o dono. Com base nestes fatos,
faça um algoritmo para ler as quantidades de pães e de broas, e depois
calcular os dados solicitados. */

int main()
{
float valor_pao, valor_broa, poupanca, valor_total;
int qtde_pao, qtde_broa;

printf("Digite a Quantidade de paes: ");
scanf("%d",&qtde_pao);
printf("Digite a Quantidade de broas: ");
scanf("%d",&qtde_broa);

valor_pao=qtde_pao*0.12;
valor_broa=qtde_broa*1.5;
valor_total = valor_pao+valor_broa;
poupanca=valor_total*0.1;

printf ("\nForam vendidos %d paes que totalizam %.2f",qtde_pao,valor_pao);
printf ("\nForam vendidas %d broas que totalizam %.2f",qtde_broa,valor_broa);
printf ("\nValor Total: %.2f",valor_total);
printf ("\nValor a ser Poupado: %.2f",poupanca);

}